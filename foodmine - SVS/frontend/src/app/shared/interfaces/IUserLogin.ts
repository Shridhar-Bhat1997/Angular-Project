// Definition of the IUserLogin interface representing user login credentials
export interface IUserLogin {
    // Email field for storing the user's email address
    email: string;
  
    // Password field for storing the user's password
    password: string;
  }
  